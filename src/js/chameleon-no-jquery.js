/**
 * For websites that already provides jQuery.
 *
 * Examples: MediaWiki (en.opensuse.org), WordPress (news.opensuse.org)
 */

window.$ = window.jQuery;
require("bootstrap");
require("./chameleon-no-bootstrap");
